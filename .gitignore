# Build output. Only /main is produced — the Makefile builds `-o main`.
# Do NOT add /api here: it would match the api/ contract directory, and the
# generated .pb.go files are meant to be committed.
/main
/bin/

# Local environment — .env.example is the committed template
.env

# air live-reload workspace
tmp/

# Test and coverage output
*.out

# macOS
.DS_Store

# local file
Project structure Golang.md
NOTES.local.md
